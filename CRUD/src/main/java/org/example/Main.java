package org.example;


import org.example.DBSObjects.DBCInventory;
import org.example.DBSObjects.DBCItem;
import org.example.DBSObjects.DBCPlayer;

import java.sql.Connection;
import java.sql.SQLException;
import java.util.List;

public class Main {
    public static void main(String[] args) throws Exception {
//        Deserializer<Player> deserializer = new Deserializer<>();
//        String filePath = "player.json";
//        List<Player> deserializedObjects = deserializer.deserialize(filePath, Player.class);
//        DBCPlayer player = new DBCPlayer(Connect.get_connection());
//        for (Player obj : deserializedObjects) {
//            player.addPlayer(obj);
//        }
        Console.start();
    }
}